[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "geoseeq"
version = "0.7.20"
authors = [
  { name="David C. Danko", email="dcdanko@biotia.io" },
]
description = "GeoSeeq command line tools and python API"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "requests",
    "click",
    "pandas",
    "biopython",
    "tqdm",
    "xxhash",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-mock>=3.10",
    "requests-mock>=1.11",
    # Azure up/download tests exercise the real SDK; kept out of the base install.
    "azure-storage-blob>=12.19",
]
s3 = [
    "boto3>=1.28",
]
azure = [
    "azure-storage-blob>=12.19",
]

[project.urls]
Homepage = "https://github.com/biotia/geoseeq_api_client"
Issues = "https://github.com/biotia/geoseeq_api_client/issues"

[project.scripts]
geoseeq = "geoseeq.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --cov=geoseeq"

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-cov>=5.0.0",
]
